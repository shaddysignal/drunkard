<h1>Edit Ingredient</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.ingredient_path(@conn, :update, @ingredient)) %>

<span><%= link "Back", to: Routes.ingredient_path(@conn, :index) %></span>
